#pragma once

#ifndef DEFINES_HPP
#define DEFINES_HPP

#define ENABLE_ASSERT

#include <string>
#include <vector>
#include <map>

using std::string;
using std::vector;
using std::map;

typedef vector<string> StringVector;
typedef map<string,string> StringMap;



#endif